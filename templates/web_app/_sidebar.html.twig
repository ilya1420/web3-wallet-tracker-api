<aside class="sidebar">
  <a class="brand" href="{{ path('app_dashboard') }}"><span class="brand-mark">WC</span><span>Wallet Console</span></a>
  <nav class="nav-list">
    <a class="{{ active == 'overview' ? 'is-active' : '' }}" href="{{ path('app_dashboard') }}">Overview</a>
    <a class="{{ active == 'wallet_new' ? 'is-active' : '' }}" href="{{ path('app_wallet_new') }}">Add wallet</a>
  </nav>
  <form method="post" action="{{ path('app_web_logout') }}">
    <input type="hidden" name="_token" value="{{ csrf_token('web_logout') }}">
    <button class="button button-muted full" type="submit">Sign out</button>
  </form>
</aside>
