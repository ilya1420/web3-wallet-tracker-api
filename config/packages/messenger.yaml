framework:
  messenger:
    transports:
      async:
        dsn: '%env(MESSENGER_TRANSPORT_DSN)%'
        serializer: App\Infrastructure\Messaging\Transport\JsonMessengerSerializer

    routing:
      App\Infrastructure\Messaging\Message\SendLoginLinkEmailMessage: async
